<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="expo.modules.liveupdates">

    <uses-sdk android:minSdkVersion="24" />

    <!-- ✅ Correct permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- ⚠️ ADD: Version check for FOREGROUND_SERVICE_SPECIAL_USE -->
    <!-- This permission is only for Android 14+ (API 34+) -->
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"
        android:minSdkVersion="34" />

    <application>
        <!-- ✅ Stopwatch Service looks good -->
        <service
            android:name=".LiveStopWatchService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="stopwatch" />
        </service>

        <!-- ✅ Timer Service looks good -->
        <service
            android:name=".LiveTimerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="timer" />
        </service>

        <!-- ✅ Task Service -->
        <service
            android:name=".LiveTaskService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="task" />
        </service>

        <!-- ✅ Notification receiver looks good -->
        <receiver
            android:name=".NotificationDismissedReceiver"
            android:exported="false" />
    </application>

</manifest>